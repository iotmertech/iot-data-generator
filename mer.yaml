protocol: mqtt

target:
  broker: mqtt://broker.emqx.io:1883
  topic: "mer/device/{device_id}/telemetry"
  client_id: "mer-client-{device_id}"
  qos: 1
  retain: false

device:
  count: 5
  id_prefix: device

payload:
  mode: random

run:
  total_messages: 20
  interval_ms: 500
