protocol: tcp

target:
  host: localhost
  port: 9000
  timeout_secs: 5
  line_delimiter: true

device:
  count: 2
  id_prefix: node

payload:
  mode: random

run:
  total_messages: 10
  interval_ms: 1000
